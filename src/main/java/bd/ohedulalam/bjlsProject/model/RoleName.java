package bd.ohedulalam.bjlsProject.model;
/*
* We want to keep a fixed set of pre-defined role
* So, we created this enum which is used by Role model
* we defined here two pre-defined role name
* we have to insert these two role name into database
*/
public enum RoleName {
    ROLE_MEMBER,
    ROLE_ADMIN
}
